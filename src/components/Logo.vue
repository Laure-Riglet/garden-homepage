<template>
	<div class="logo">arboretum</div>
</template>

<style scoped>
.logo {
	display: flex;
	align-items: center;
	color: var(--color-white);
	font-size: calc(28 * var(--average-ratio));
	font-weight: 500;
	letter-spacing: 0;
	margin: 0 calc(50 * var(--width-ratio)) calc(3 * var(--height-ratio));
	width: auto;
}
</style>
